<Window x:Class="PlaylistDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Playlist Downloader" Height="350" Width="525"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:carViewer="clr-namespace:PlaylistDownloader"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type={x:Type carViewer:MainWindow}}" Icon="icon.ico">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="367*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="20"/>
			<RowDefinition Height="40"/>
		</Grid.RowDefinitions>
		<ProgressBar Grid.Row="1" Value="{Binding ProgressValue}" IsIndeterminate="{Binding IsIndeterminate}" />

		<TextBox Margin="0 0 0 5" Text="{Binding PlayList}" Grid.Row="0" Grid.Column="0" IsManipulationEnabled="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" PreviewMouseDown="TextBoxMouseDown" />

		<Button Grid.Row="2" Click="DownloadButtonClick" Grid.Column="0">Download Songs</Button>
	</Grid>
</Window>
